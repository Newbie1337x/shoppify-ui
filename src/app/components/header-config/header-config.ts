import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-header-config',
  imports: [RouterLink],
  templateUrl: './header-config.html',
  styleUrl: './header-config.css'
})
export class HeaderConfig {

}
